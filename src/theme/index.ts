import { palette } from "./palette";

export const theme = {
    ...palette,
    tabIconDefault: palette.lightGray,
    tabIconSelected: palette.white,
    tabBar: palette.whiteTint,
    error: palette.red,
    warningBackground: palette.yellowTint,
    warningText: palette.mudYellow,
    errorText: palette.white,
    noticeBackground: palette.lightBlue,
    tintColor: palette.lightBlue,
};
